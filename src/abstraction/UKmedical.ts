export interface UKmedical{

    //only method declaration - no method body
    //cannot create the object/instance of the interface
    oncology(): void;
    pedia(): void;
    dental(): void;
    emergency(): void;
}